StylesPath = .vale-styles
MinAlertLevel = warning
Vocab = Brand

[*.{md,qmd}]
BasedOnStyles = Vale
# Vale's built-in style checks repetition, consecutive-word duplication and
# a few common issues without needing external style packages.
